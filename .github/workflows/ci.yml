name: CI

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-check:
    runs-on: windows-latest
    steps:
      - name: Check out source
        uses: actions/checkout@v6

      - name: Set up .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: |
          dotnet restore Node.csproj
          dotnet restore Checks/Node.Checks.csproj

      - name: Build application
        run: dotnet build Node.csproj -c Release --no-restore

      - name: Run checks
        run: dotnet run --project Checks/Node.Checks.csproj -c Release --no-restore
